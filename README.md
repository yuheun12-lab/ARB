# ARB SCAN Mobile

아이폰에서 앱처럼 쓰는 업비트/빗썸 가격차 순위 웹앱입니다.

## Render 배포 설정

Build Command:
pip install -r requirements.txt

Start Command:
gunicorn app:app --bind 0.0.0.0:$PORT

## 아이폰에서 앱처럼 쓰기

1. Render 주소를 Safari로 열기
2. 공유 버튼
3. 홈 화면에 추가
